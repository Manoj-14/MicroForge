{{- if .Values.loginMysql.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.loginMysql.name }}-secret
  namespace: {{ include "microforge.namespace" . }}
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: {{ .Values.loginMysql.secrets.rootPassword }}
{{- end }}

