FROM mysql

ENV MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
ENV MYSQL_DATABASE=${MYSQL_DATABASE}
ENV MYSQL_USER=${MYSQL_USER}

EXPOSE 3306

COPY empdir_users.sql /docker-entrypoint-initdb.d/
COPY empnotification_notifications.sql /docker-entrypoint-initdb.d/